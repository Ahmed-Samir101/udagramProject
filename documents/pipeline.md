### Pipeline

```
- Pipeline setup environments node, rb and aws-cli in orbs.

- The pipeline first install the dependencies, build the application, then deploy it when it ensures that it works successfully.


```